[target.'cfg(all(target_arch = "x86_64", target_os = "none"))']
rustflags = ["--cfg", "getrandom_backend=\"custom\""]

# If you are specifically targeting UEFI, use this instead:
[target.x86_64-unknown-uefi]
rustflags = ["--cfg", "getrandom_backend=\"custom\""]